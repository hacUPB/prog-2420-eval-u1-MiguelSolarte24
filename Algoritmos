
6.) calculamos_edad =

    fecha_nacimiento = año_nacimiento, mes_nacimiento, dia_nacimiento
    fecha_actual =  año_actual, mes_actual, dia_actual

    
   Calculamos la diferencia en años
    edad = año_actual - año_nacimiento

   Revisar si el cumpleaños de este año ya pasó 
    (mes_actual < mes_nacimiento) 
      (dia_actual < dia_nacimiento)
       
    
    Calculamos el mes y los días restantes
    fecha_cumpleanos_proxima = (año_actual, mes_nacimiento, dia_nacimiento)
    fecha_actual < fecha_cumpleaños
            
    dias_restantes = (fecha_actual - fecha_cumpleaños_proxima)
    

si el dia_actual = dia_nacimiento  y  mes_actual = mes_nacimiento

    Hoy_es_su_cumpleaños  



	4. Distancia que recorre el vehiculo 

calcular_distancia_total

(velocidad) = (tiempo recorrido)

        velocidad [Vel]
        Tiempo[T]

distancia_total = velocidad * tiempo
distancia_total  = x 
distancia_total = distancia_inicial + distancia recorrida
    IMPRIMIR distancia_total
     
1. determinamos_aprobacion de notas
  
verificamos_aprobación 

total_calificaciones= S
numero_calificaciones= N

Calculamos promedio =
S / N

Promedio > 3.0 aprobado  
IMPRIMIR Ana_Aprobó_el_curso 

Promedio < 3.O reprobado 
IMPRIMIR Ana_reprueba_el_curso 
